// Site-wide constants
export const SITE_NAME = 'GejorDev'
export const SITE_URL = 'https://gejordev.com'

// Navigation IDs (for anchor links)
export const NAV_IDS = {
  HERO: 'hero',
  ABOUT: 'about',
  STACK: 'stack',
} as const

// Contact link types
export const CONTACT_TYPES = {
  LINKEDIN: 'linkedin',
  EMAIL: 'email',
  PHONE: 'phone',
} as const

// Language codes
export const LANGUAGES = {
  ES: 'es',
  EN: 'en',
} as const

export const DEFAULT_LANGUAGE = LANGUAGES.ES
